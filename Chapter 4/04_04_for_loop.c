#include<stdio.h>

int main(){
    for(int i=0; i<10; i++){
        printf("The value of i is %d\n", i);//Now it's print 0 to 9, if we add somrthing like (i+1)then it will print 1 to 10.
    }                                        // Only printf option e uporare theory effectable-->printf("The value of i is %d\n", i+1);
    return 0;
}